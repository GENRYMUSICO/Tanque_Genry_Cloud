def iniciar_tray():
    import pystray
    from PIL import Image
    try:
        # Carga el logo que el mismo programa descargó de la nube
        image = Image.open("LOGO AV GENRY OFICIAL.png")
        menu = pystray.Menu(
            pystray.MenuItem(f"Usuario: {FONETICA_NOMBRE}", lambda: None),
            pystray.MenuItem("Búnker: Protegido", lambda: None),
            pystray.MenuItem("Desactivar Protección", confirmar_salida_premium)
        )
        icon = pystray.Icon("AV_Genry", image, "AV GENRY - Made in Perú", menu)
        icon.run()
    except Exception as e:
        print(f"Error al iniciar el tray: {e}")
